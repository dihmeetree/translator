# Twitch Chat Translator Configuration
# Copy this file to .env and fill in your values

# Required: The Twitch channel to join (without the #)
TWITCH_CHANNEL=channel_name

# Optional: Twitch OAuth token for authenticated access
# Get one from: https://twitchapps.com/tmi/
# Leave empty for anonymous read-only access
# TWITCH_OAUTH_TOKEN=your_oauth_token_here

# Optional: Twitch username (defaults to anonymous)
# TWITCH_USERNAME=your_username

# Translation backend: "google" (default) or "libretranslate"
# Google uses your translation-api server (includes romanization/pronunciation)
# LibreTranslate: docker run -d -p 5000:5000 libretranslate/libretranslate --load-only ru,en
TRANSLATION_BACKEND=google

# Translation API server URL
# For Google backend: URL of your translation-api server
# For LibreTranslate: URL of LibreTranslate instance
TRANSLATION_API_URL=http://localhost:4000

# Optional: Minimum confidence for language detection (0.0 to 1.0)
# Lower values = more messages translated (but potentially incorrect)
# Higher values = more accurate but might miss some non-English messages
# Default: 0.5
# DETECTION_CONFIDENCE=0.5

# Optional: Default source language for translation
# When set, ALL messages will be translated from this language to English
# This bypasses language detection entirely
# Common codes: ru (Russian), es (Spanish), fr (French), de (German),
#               ja (Japanese), ko (Korean), zh (Chinese), pt (Portuguese)
# DEFAULT_LANGUAGE=ru

# Optional: Maximum translation cache size in megabytes
# When exceeded, oldest entries are automatically purged
# Default: 50
# CACHE_SIZE_MB=50

# === Speech-to-Text Transcription (Optional) ===
# Deepgram API key for real-time audio transcription of the stream.
# Sign up at https://deepgram.com to get a free API key.
# When set, the app will capture stream audio and display live transcriptions.
# Requires 'streamlink' and 'ffmpeg' to be installed:
#   pip install streamlink
#   https://ffmpeg.org/download.html
# DEEPGRAM_API_KEY=your_deepgram_api_key_here

# Deepgram model to use (default: nova-2)
# Options: nova-2 (best general), nova-2-general, nova-2-meeting, etc.
# DEEPGRAM_MODEL=nova-2

# Language for speech-to-text (defaults to DEFAULT_LANGUAGE if set, otherwise "multi")
# Use "multi" for automatic language detection.
# Or specify a language code: en, ru, es, fr, de, ja, ko, zh, etc.
# STT_LANGUAGE=multi

# === Web UI (Optional) ===
# Port for the web UI HTTP/WebSocket server.
# Open http://localhost:3000 in your browser for a live translation dashboard.
# Default: 3000
# WEB_PORT=3000

# Logging level (optional)
# RUST_LOG=twitch_translator=debug
